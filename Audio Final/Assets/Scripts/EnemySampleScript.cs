using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemySampleScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}

	void SampleEnemyDetectFunction ()
	{
		//if EnemyType = 1, play 1audio;
		//if EnemyType = 1 && playerGun = 1 && inRange && isLooking, cause damage
		//etc. 
		// do this for all audio types. 
	}
}
